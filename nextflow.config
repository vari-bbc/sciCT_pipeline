params.{
    output_dir = "results"
}
conda {
    enabled = true
}
process {
    conda = "envs/environment.yml"
    withName: RUN_DEMUX {
        publishDir = [ path: params.output_dir, mode: 'copy', overwrite: true ]
  }
}